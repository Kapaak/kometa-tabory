import styled from 'styled-components';

export const CampSectionError = styled.div`
  display: grid;
  place-content: center;
  height: 40rem;
  gap: 1.6rem;
`;

export const ButtonContainer = styled.div`
  margin-inline: auto;
`;

export const CampSectionErrorTitle = styled.h2`
  color: var(--colr);
  font-size: 2rem;
`;
